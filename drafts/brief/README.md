# drafts/brief/ — the structured client brief lives here as brief.md

Draft it with Claude from transcript/notes using `brief-template.md` (in this folder — the canonical 11-section structure; workflow/01 Phase 1). Assumptions marked ⚠, §11 open questions = the human sign-off surface.

> **Upstream stage — in-repo today.** This is one of the upstream stages (brief → wireframe → copy) that run in-repo per HQ `workflow/01`; the Hub pipeline is the target (HQ `workflow/27`). Its output is synthesised into the **`/project-input/`** handoff that the build stage actually reads (`AGENTS.md`). Not read by the build directly.
