# drafts/content/ — copy passes

Run the /copy pattern (HQ research 2026-06-06 §b): objection map in, coverage map out; numbers or [NEEDS CLIENT DATA]; human picks the headline. copy-NN.md is the source of truth the WP seed and fallbacks mirror.

> **Upstream stage.** Copy is drafted here (the `/copy` skill IS a build skill — see `AGENTS.md`) and landed into the WP seed + `site.config.ts`. Related upstream stages — brief (`drafts/brief/`) and wireframe (`drafts/wireframe/`) — feed the **`/project-input/`** handoff; the Hub pipeline is the target for all three (HQ `workflow/27`). See `drafts/README.md`.
