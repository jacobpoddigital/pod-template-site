# All non-empty tags — the filter UI's expandable tag list + tag archive static
# params (workflow/34).
query AllTags {
  tags(first: 200, where: { hideEmpty: true }) {
    nodes {
      databaseId
      name
      slug
      count
    }
  }
}
