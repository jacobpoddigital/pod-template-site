# One tag by slug — the archive header (name + description) + post count. workflow/34.
query TagBySlug($slug: ID!) {
  tag(id: $slug, idType: SLUG) {
    databaseId
    name
    slug
    description
    count
  }
}
